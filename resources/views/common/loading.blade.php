{{-- ============================================================
     Loading Spinner — Full-Screen Overlay Component
     ============================================================
     Renders a triple-ring animated spinner with blue glow.
     Hidden by default; toggled via jQuery show/hide on
     form submissions and page navigation events.
     ============================================================ --}}

<div id="custom-loader" class="loader-overlay" style="display: none;">
    <div class="spinner"></div>
</div>

{{-- Loader styles are linked from public/css/loader.css via the layout --}}
